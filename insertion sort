def isort(A):
    for i in range(1, len(A)):
        j = i
        while A[j - 1] > A[j] and j > 0:
            A[j - 1], A[j] = A[j], A[j - 1]
            j -= 1

if __name__ == "__main__": 
    A = [4, 3, 6, 2, 9, 7, 1, 8, 5]
    isort(A)
    print(A)
